# Agent 记忆管理微服务 - 总体施工计划

> **关联设计文档:** `docs/设计.md`
> **团队人数:** 3 人
> **估算总工时:** 10-14 h/人

---

## 任务总览

| # | 任务 | 执行者 | 预估 | 产出 |
|---|------|--------|------|------|
| 1 | 项目脚手架 | 全员 | 1h | pyproject.toml, requirements.txt, .env.example, Dockerfile, docker-compose.yml |
| 2 | 数据库基础 | Dev A | 2h | config.py, database.py, models/database.py, docker/init.sql |
| 3 | 高阶 SQL 对象 | Dev A | 2h | sp_compose_context, 2触发器, 2视图, 6索引 |
| 4 | Agent & Skill API | Dev B | 3h | routers/{agents,skills}.py + services |
| 5 | User Memory API | Dev C | 3h | routers/users.py + user_service.py |
| 6 | Session & Message API | Dev A | 3h | routers/sessions.py + session_service.py |
| 7 | Context Composition API | Dev A | 2h | routers/context.py + context_service.py |
| 8 | LLM 模块 | Dev B | 3h | llm/{provider,extractor,summarizer,embedder}.py |
| 9 | FastAPI App 组装 | Dev A | 1h | main.py, middleware, 异常处理 |
| 10 | Python SDK | Dev C | 3h | sdk/ 包 (MemoryClient, Session, Context) |
| 11 | 调试前端 | Dev C | 2h | static/debug.html |
| 12 | 集成验证 | 全员 | 2h | docker compose 验证 + API 测试 |

---

## 依赖关系图

```
Task 1 (Scaffold) ──────────→ Task 2 (DB Foundation)
                                      │
                            ┌─────────┼─────────┐
                            ▼         ▼         ▼
                        Task 3    Task 4    Task 5
                       (高级SQL)  (Agent)   (User)
                       (Dev A)   (Dev B)   (Dev C)
                            │         │         │
                            │         │    ┌────┘
                            ▼         ▼    ▼
                        Task 6    Task 8   Task 10
                       (Session) (LLM)   (SDK)
                       (Dev A)   (Dev B) (Dev C)
                            │         │         │
                            ▼         │         ▼
                        Task 7        │     Task 11
                       (Context)      │   (Frontend)
                       (Dev A)        │   (Dev C)
                            │         │         │
                            ▼         │         │
                        Task 9        │         │
                       (App组装)      │         │
                       (Dev A)        │         │
                            │         │         │
                            └─────────┼─────────┘
                                      ▼
                                Task 12
                            (集成验证, 全员)
```

---

## 阶段一：基础设施（全员串行）

**执行: 全体成员 / 预估: 1h**

### Task 1: 项目脚手架

**产出文件:**
- `pyproject.toml`
- `requirements.txt`
- `.env.example`
- `Dockerfile`
- `docker-compose.yml`
- 更新 `.gitignore`

**操作步骤:**
1. 由一人创建全部文件，另两人 code review
2. `docker compose build` 验证镜像构建成功

**完成状态:** ✅ 已完成 (2026-06-29)

**实际产出:**
- `pyproject.toml` — 7 个依赖声明 (fastapi, uvicorn, sqlalchemy, asyncpg, httpx, pgvector, python-dotenv)
- `requirements.txt` — uv export 生成，29 个精确版本锁定
- `.env.example` — 5 个环境变量模板
- `Dockerfile` — python:3.13-slim，uvicorn 入口
- `docker-compose.yml` — db (pgvector:pg16) + api 双容器编排，healthcheck 依赖等待
- `.gitignore` — 追加 __pycache__/ .env *.egg-info dist/ build/

**验证结果:** `uv sync` 安装 27 个包成功；全部 7 个依赖 Python import 通过；`docker compose config` 语法正确；`docker compose pull db` 镜像拉取成功。

---

## 阶段二：数据库层（并行前期）

### Task 2: 数据库基础

**执行: Dev A / 预估: 2h**

**产出文件:**
- `src/__init__.py`
- `src/config.py` — 环境变量读取
- `src/database.py` — async SQLAlchemy 引擎 + session 工厂
- `src/models/__init__.py`
- `src/models/database.py` — 8 张表 ORM 模型
- `docker/init.sql` — DDL 脚本

**阻塞:** Dev B / Dev C 等待 Task 2 完成后才能开始 Task 4 / Task 5。
**建议:** Dev B / Dev C 在等待期间阅读 `docs/设计.md` 第四章 API 契约，提前定义 Pydantic request/response schema。

**完成状态:** ✅ 已完成 (2026-06-29)
- 9 张表 ORM 模型 (pgvector.sqlalchemy.Vector, DeclarativeBase, 无 relationship)
- docker/init.sql: DDL + 8 个自定义索引
- 验证: 9 表 17 索引创建成功, pgvector embedding 往返正确, ORM CRUD 全部通过

---

## 阶段三：核心 API + 高级 SQL（3 人并行）

### Task 3: 高阶 SQL 对象

**执行: Dev A / 预估: 2h**

**产出文件:**
- `docker/init.sql`（追加）

**内容:**
- `sp_compose_context` — 存储过程
- `tg_check_message_threshold` — 消息超阈值触发器
- `tg_audit_log` — DML 审计触发器
- `v_context_preview` — 上下文预览视图
- `v_memory_stats` — 存储统计视图

**完成状态:** ✅ 已完成 (2026-06-29)
- 1 存储过程 + 2 触发器函数 + 3 触发器 + 2 视图
- 后续增强: `sp_compose_context` 新增 `p_query_embedding` 可选参数（LLM 向量检索汇入点）+ `message_count` 字段

---

### Task 4: Agent & Skill API

**执行: Dev B / 预估: 3h**

**产出文件:**
- `src/services/agent_service.py`
- `src/services/skill_service.py`
- `src/routers/agents.py`
- `src/routers/skills.py`

**端点实现:**
- `POST/GET/PATCH /api/v1/agents`
- `POST/GET/DELETE /api/v1/agents/{id}/skills`
- `POST/GET/GET{id}/PATCH/DELETE /api/v1/skills`

**完成状态:** ✅ 已完成 (2026-06-29)
- AgentService 7 方法（显式参数，UUID 类型，前置验证）；SkillService 5 方法
- 11 端点 + 5 设计决策全通过验证（201/200/204/404/409/422）
- AgentSkillResponse 含 enabled 字段

---

### Task 5: User Memory API

**执行: Dev C / 预估: 3h**

**产出文件:**
- `src/services/user_service.py`
- `src/routers/users.py`

**端点实现:**
- `GET/PATCH /api/v1/users/{id}/profile`
- `POST/GET/DELETE /api/v1/users/{id}/memories`

---

## 阶段四：扩展模块（3 人并行）

### Task 6: Session & Message API

**执行: Dev A / 预估: 3h**

**产出文件:**
- `src/services/session_service.py`
- `src/routers/sessions.py`

**端点实现:**
- `POST/GET/PATCH /api/v1/sessions`
- `POST/GET /api/v1/sessions/{id}/messages`

**完成状态:** ✅ 已完成 (2026-06-29)
- SessionService 5 方法（create/get/update/add_message/get_messages）
- 5 REST 端点 + 5 Pydantic schema
- `add_message` 内原子事务：INSERT message + UPDATE counter 自增
- 后续集成: Dev B LLM 模块通过 `BackgroundTasks` 触发 `LLMService.run_post_message`

---

### Task 7: Context Composition API

**执行: Dev A / 预估: 2h**
**依赖: Task 4, 5, 6 完成**

**产出文件:**
- `src/services/context_service.py`
- `src/routers/context.py`

**核心流程:** 调用 `sp_compose_context` → 返回三层聚合结果

**完成状态:** ✅ 已完成 (2026-06-30)
- ContextService.compose_context 双路径（向量检索 / importance 降级）
- POST /api/v1/context 端点 + 5 Pydantic schema
- `query_embedding` LLM 汇入点

---

### Task 8: LLM 模块

**执行: Dev B / 预估: 3h**

**产出文件:**
- `src/llm/__init__.py` — 重导出 4 类 + TypedDict
- `src/llm/provider.py` — OpenAI 兼容 HTTP 客户端（类级 semaphore，每调用新建 client）
- `src/llm/extractor.py` — 用户记忆提取（TypedDict 返回）
- `src/llm/summarizer.py` — 会话摘要生成
- `src/llm/embedder.py` — 文本 → 向量
- `src/services/llm_service.py` — 集成层（独立 session + BackgroundTasks）
- `src/routers/sessions.py` — 修改 add_message 端点注入 BackgroundTasks

**完成状态:** ✅ 已完成 (2026-06-29)
- 7 设计决策：每调用新建 client + 类级信号量 + 独立 DB session + Router 层集成 + LLM/Embedding 分离 + VECTOR 无维度 + memory_fragment embedding gap 修复
- 降级验证通过：无 api_key → 立即 return
- 全功能验证通过：DeepSeek Chat + GITEE AI Embedding → 摘要/提取/嵌入全部正常

---

### Task 9: FastAPI App 组装

**执行: Dev A / 预估: 1h**

**产出文件:**
- `src/main.py` — FastAPI app 创建、Router 注册、中间件、静态文件、lifespan
- `src/routers/admin.py` — health + stats 端点

**完成状态:** ✅ 已完成 (2026-06-30)
- 5 个 router 注册（agents, skills, sessions, context, admin）
- CORS + API Key 中间件
- /debug 路由 + static/debug.html 占位文件

---

### Task 10: Python SDK

**执行: Dev C / 预估: 3h**

**产出文件:**
- `sdk/setup.py`
- `sdk/requirements.txt`
- `sdk/agentmemory/__init__.py`
- `sdk/agentmemory/client.py` — MemoryClient
- `sdk/agentmemory/session.py` — Session
- `sdk/agentmemory/context.py` — Context, AgentMemory, UserMemory, SessionMemory
- `sdk/agentmemory/exceptions.py` — MemoryServiceError

---

### Task 11: 调试前端

**执行: Dev C / 预估: 2h**

**产出文件:**
- `src/static/debug.html`

**页面结构:** 三栏布局，5 Tab（Agent Info / User Memory / Session / Context Preview / LLM Logs）

---

## 阶段五：集成验证

### Task 12: 集成验证

**执行: 全体 / 预估: 2h**

**验证清单:**
- [ ] `docker compose up -d` 一键启动成功
- [ ] `curl /api/v1/health` 返回 `{"status":"ok","pgvector":true}`
- [ ] 遍历 22 个 API 端点，逐一确认请求/响应
- [ ] 存储过程调用验证：`SELECT * FROM sp_compose_context(...)`
- [ ] 触发器验证：INSERT message → 查询 audit_log 确认记录
- [ ] 视图验证：`SELECT * FROM v_memory_stats`
- [ ] 索引对比：EXPLAIN ANALYZE 建索引前后查询性能
- [ ] 全文检索对比：`to_tsvector` vs `LIKE '%keyword%'`
- [ ] 事务验证：并发写入 → 确认原子性
- [ ] SDK 端到端验证：SDK 调用 → API → DB 完整链路
- [ ] 调试前端验证：浏览器打开 `/debug`，切换 Tab，发送请求
- [ ] LLM 模块（如已配置 API Key）：确认摘要/提取/嵌入功能
